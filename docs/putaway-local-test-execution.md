# 上架专项本地执行记录

## 执行范围

- 环境：本地 Django + MySQL 测试库，使用 `--reuse-db --no-migrations`。
- 自动化：上架任务生成、PDA 权限与状态、幂等、并发、审核、入库单关闭、库存移动与失败回滚。
- 静态检查：新增测试、迁移和压测脚本通过 Ruff；`git diff --check` 通过；tasking 迁移无漂移。

## 执行结果

修复后相关回归共执行56项，全部通过。序列号上架用例已移除 `xfail`，并验证同一库存行
迁移到目标库位、成对流水和库存总量不变。

现有正式入库至出库闭环用例
`test_flow_10_formal_inbound_putaway_to_outbound_full_chain` 已通过；非 `OK` 幂等标记和历史扫描
不再参与自动过账，显式传入非法扫描时仍保持严格拒绝。

已通过范围包括：任务幂等生成、无正数流水不生成、状态与审核约束、仓库/货主隔离、库位过滤、领取互斥、分次上架、数量边界、目标库位锁定、请求幂等冲突、MySQL 并发防超量、审计日志、批次与效期继承、成对流水、库存不足回滚、第二条流水写入失败后的原子回滚及重试、关联任务全部过账后关闭入库单。

## 当前结论

已修复本地自动化发现的两个上线阻断项。本地专项和业务闭环通过，但预生产30并发、Android PDA
真机、完整仓储回归、覆盖率及全局库存对账尚未执行，因此当前结论仍为：**待预生产验收**。

完整 `allapp/test_business_flows.py` 已执行：12项中6项通过、6项失败。失败集中在既有出库仓库确认、
取消后任务行保留策略和账单查看权限断言，与本次上架库存变更无关；上架验收指定的 flow 10 已通过。
这些基线失败仍需由对应出库、权限和计费模块维护者收敛后，才能满足全仓回归零失败门禁。

## 尚未在本地替代执行的验收

- 两种 Android PDA 分辨率、弱网/断网恢复和 H5 冒烟。
- 预生产 30 操作员并发及 100 行/1000 任务池容量测试。
- 生产同构环境的 MySQL、Redis、权限配置与全局库存对账。
- 完整仓储回归、覆盖率门禁和现场业务签字。
