# Release remediation evidence index

- Baseline commit: `2d48512cf086`
- Worktree: dirty; this directory is an interim local verification index, not a releasable SHA artifact
- Date: 2026-08-20 (Asia/Shanghai)
- Database: isolated local MySQL 8.0.46 schema `wms_db_test`
- Cache: isolated local Redis 7.4.7
- Python: local `.venv` 3.12.3; CI/container contract 3.12.13

## Verified locally

- Clean MySQL migration graph: passed (approximately 2,300 seconds).
- Regular backend serial suite, excluding the dedicated identifier migration job:
  `1044 passed, 943 subtests passed, 14 warnings in 9510.77s`.
- Four-worker MySQL coverage suite: `1044 passed, 943 subtests passed in 14947.17s`;
  production-code coverage `71.69%` (hard gate 70%). Evidence: `.coverage` and `coverage.xml`.
- Dedicated product identifier migration job: `2 passed in 269.56s`.
- JWT minimum-key regression with the warning promoted to an error: `5 passed in 18.08s`.
- Owner client: 81 unit tests passed; H5 build passed; the former flaky inventory case
  passed 20 consecutive runs (140 assertions).
- Boss client: 9 unit tests passed; H5 build passed; Playwright 1/1 passed.
- PDA baseline: 37 unit tests passed; H5 build passed; Playwright 8/8 passed.
- Production-code Bandit scan: 0 high, 0 medium, 25 low.
- Python production dependency audit: one no-fix WeasyPrint finding retained in
  `pip-audit-prod.json`; the approved time-bounded gate result is in
  `pip-audit-prod-gated.json` (`0` unhandled, `1` precisely ignored).
- Django system check, migration drift check, lint ratchet, shell syntax, CI YAML parse,
  and Git whitespace check passed at the recorded checkpoints.

## Evidence not available yet

This interim directory intentionally contains no fabricated PASS artifacts for Trivy, candidate-image
smoke, real AppIDs/WeChat builds, signed Android artifacts, physical PDA and
printer runs, payment merchant tests, 50/100-user performance tests, system-level fault injection,
or MySQL/media recovery drills. Those hard gates must be generated by the final commit SHA pipeline
and staging environment before release.

The detailed and authoritative status is in
`docs/comprehensive-test-report-2026-08-20.md`, section 7.
